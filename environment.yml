name: agriculture-vlab
channels:
  - conda-forge
dependencies:
  - click >=8.0
  - numpy >=1.17
  - pandas >=1.3
  - pyproj  # use xcube's version constraints
  - xarray >=0.18.2
  - xcube >=0.9.2
  - zarr >=2.8
  # Testing
  - pytest >=4.4
  - pytest-cov >=2.6
